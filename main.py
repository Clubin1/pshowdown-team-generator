"""
Pokemon showdown randomizer 
the args we want to accept are the generation of the pokemon.
-g 1-9 
   generate a random pokemon of that generation 
if the flag is -g all, it will generate a random pokemon of any generation 
generation works by generating a random number between the start and end of the generation pokemon IDs for example generation 1 has pokemon IDs 1-150 

-b true/false will determine if baby pokemon are allowed in the generated list
all pokemon movesets, stats, items are taken from smogon.com
"""

def main():
    pass 
